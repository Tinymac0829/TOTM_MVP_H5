# ENG-04 × ENG-05 真实浏览器联调记录模板

**文档类型**：L1 联调记录模板  
**关联主文档**：[联合验收功能卡](./eng04_eng05_joint_acceptance_card.md)  
**关联执行清单**：[短版执行清单](./eng04_eng05_joint_acceptance_checklist.md)  
**创建日期**：2026-04-24  
**最后更新**：2026-05-01
**用途**：记录真实浏览器联调过程、问题、回归结果与收口结论

---

## 1. 本次联调信息

| 字段 | 内容 |
|---|---|
| 日期 |  |
| 执行人 |  |
| 分支 | `codex/eng-05` |
| 关联任务 | `ENG-04`, `ENG-05` |
| 设备 |  |
| 系统版本 |  |
| 浏览器 |  |
| 浏览器版本 |  |
| 屏幕分辨率 |  |
| 方向 | 竖屏 |
| 构建方式 | 本地 HTTP / 其他 |

### 本地 HTTP 启动方式

真实浏览器联调时，优先由执行人在本机 PowerShell 手动启动静态服务，并保持该 PowerShell 窗口打开：

```powershell
cd E:\GameDev\H5\Project\TOTM_MVP\TOTM_MVP_Dev_worktrees\ENG-04
python -m http.server 8094 --bind 127.0.0.1
```

浏览器访问：

```text
http://127.0.0.1:8094/
http://127.0.0.1:8094/?stage=eng04_death_validation
```

如果 `8094` 被占用，将命令和浏览器 URL 中的端口同步改为其他空闲端口，例如 `8095`。

## 2. 测试前确认

- [ ] 当前代码已更新到待验收版本
- [ ] `git status` 已确认
- [ ] 当前环境能正常打开页面
- [ ] 本地 HTTP 服务由 PowerShell 手动启动，且服务窗口保持打开
- [ ] 本次验收口径已与联合验收功能卡对齐
- [ ] 已知上游风险：`ENG-04` 仍可能后续调整

## 3. 本次执行结果摘要

| 项目 | 结果 | 备注 |
|---|---|---|
| 启动链路 | `PASS / FAIL / BLOCKED` |  |
| 滑行手感主链路 | `PASS / FAIL / BLOCKED` |  |
| HUD 顶部计数 | `PASS / FAIL / BLOCKED` |  |
| 失败弹窗链路 | `PASS / FAIL / BLOCKED` |  |
| 通关弹窗链路 | `PASS / FAIL / BLOCKED` |  |
| 重开链路 | `PASS / FAIL / BLOCKED` |  |
| 重复游玩 fallback | `PASS / FAIL / BLOCKED` |  |
| 点击区域与缩放 | `PASS / FAIL / BLOCKED` |  |
| UI 不遮挡可玩区 | `PASS / FAIL / BLOCKED` |  |
| 异常恢复 | `PASS / FAIL / BLOCKED` |  |

## 4. 详细记录

### 4.1 启动链路

| 步骤 | 观察结果 | 判定 | 备注 |
|---|---|---|---|
| 打开页面后是否进入开始界面 |  |  |  |
| 点击开始后是否进入加载态 |  |  |  |
| 加载完成后是否进入 `story_001` |  |  |  |
| 进入玩法后是否能正常移动 |  |  |  |

### 4.2 滑行与输入

| 步骤 | 观察结果 | 判定 | 备注 |
|---|---|---|---|
| 四向滑行直到受阻 |  |  |  |
| 输入缓冲体感是否正常 |  |  |  |
| 弹窗期间输入是否被屏蔽 |  |  |  |
| 是否存在误触、残留输入或时序异常 |  |  |  |

### 4.3 HUD 与收集物

| 步骤 | 观察结果 | 判定 | 备注 |
|---|---|---|---|
| Dot 收集与顶部计数同步 |  |  |  |
| Coin 收集与顶部计数同步 |  |  |  |
| Star 收集与顶部计数同步 |  |  |  |
| HUD 是否始终位于最上层 |  |  |  |
| HUD 是否遮挡可玩区 |  |  |  |

### 4.4 失败链路

| 步骤 | 观察结果 | 判定 | 备注 |
|---|---|---|---|
| 触发死亡后是否进入失败弹窗 |  |  |  |
| 弹窗淡入期间按钮是否锁定 |  |  |  |
| 点击“重新开始”后是否回到 Enter |  |  |  |
| 收集物是否恢复 |  |  |  |
| HUD 计数是否清零 |  |  |  |
| 是否满足失败后 `2s` 内可重开 |  |  |  |

### 4.5 通关闭环

| 步骤 | 观察结果 | 判定 | 备注 |
|---|---|---|---|
| 进入 Exit 后是否先完成移动收尾 |  |  |  |
| 是否正确进入通关弹窗 |  |  |  |
| 通关结算计数是否正确 |  |  |  |
| 当前仅 `story_001` 时是否走重复游玩 fallback |  |  |  |
| 再次进入后是否可继续游玩 |  |  |  |

### 4.6 点击区域与缩放

| 步骤 | 观察结果 | 判定 | 备注 |
|---|---|---|---|
| 按钮点击区域是否与视觉位置一致 |  |  |  |
| 不同窗口尺寸下是否仍准确 |  |  |  |
| 是否存在明显点击偏移 |  |  |  |

### 4.7 异常恢复

| 步骤 | 观察结果 | 判定 | 备注 |
|---|---|---|---|
| 加载异常时是否可恢复 |  |  |  |
| 是否有不可交互卡死 |  |  |  |
| 错误提示是否可理解 |  |  |  |

## 5. 问题列表

| ID | 严重级别 | 模块 | 现象 | 复现步骤 | 预期 | 实际 | 是否阻塞 |
|---|---|---|---|---|---|---|---|
|  | `P0/P1/P2/P3` |  |  |  |  |  |  |
|  | `P0/P1/P2/P3` |  |  |  |  |  |  |
|  | `P0/P1/P2/P3` |  |  |  |  |  |  |

## 6. 回归记录

| 日期 | 修改点 | 回归范围 | 结果 | 备注 |
|---|---|---|---|---|
|  |  |  | `PASS / FAIL` |  |
|  |  |  | `PASS / FAIL` |  |

## 7. 结论

### 本轮判断

- `ENG-04` 当前状态：`可继续 / 需回调 / 阻塞`
- `ENG-05` 当前状态：`可继续 / 需回调 / 阻塞`
- 是否允许进入下一轮联调：`是 / 否`
- 是否允许进入最终验收准备：`是 / 否`

### 建议动作

- [ ] 保持当前实现，进入下一轮真实浏览器验证
- [ ] 优先修复 `ENG-04` 问题，再回归 `ENG-05`
- [ ] 优先修复 `ENG-05` 问题，再继续联调
- [ ] 需要同步更新联合验收口径文档
- [ ] 需要同步更新执行计划 / worktree 台账

## 8. 附加说明

```text

```

---

## 9. 已执行联调记录：2026-04-26

### 9.1 本次联调信息

| 字段 | 内容 |
|---|---|
| 日期 | `2026-04-26` |
| 执行人 | 用户真实浏览器人工验收，Codex 记录与问题修复 |
| 分支 | `codex/eng-04`（包含 `dfe6667` 的 ENG-05 阶段成果） |
| 关联任务 | `ENG-04`, `ENG-05` |
| 设备 | Windows PC |
| 系统版本 | 未记录 |
| 浏览器 | Chrome / Edge 类真实浏览器 |
| 浏览器版本 | 未记录 |
| 屏幕分辨率 | 多窗口尺寸与浏览器缩放场景 |
| 方向 | 竖屏验证为主，窗口缩放补充检查 |
| 构建方式 | PowerShell 手动启动本地 HTTP |

### 9.2 测试前确认

- [x] 当前代码已更新到待验收版本
- [x] `git status` 已确认
- [x] 当前环境能正常打开页面
- [x] 本地 HTTP 服务由 PowerShell 手动启动，且服务窗口保持打开
- [x] 本次验收口径已与联合验收功能卡对齐
- [x] 已知上游风险已处理：收集物提前消失问题已修复并回归通过

### 9.3 本次执行结果摘要

| 项目 | 结果 | 备注 |
|---|---|---|
| 启动链路 | `PASS` | 默认显示开始界面；点击后进入 loading 并成功进入 `story_001`。Loading 时间很短，非阻塞。 |
| 滑行手感主链路 | `PASS` | 四向滑行正常；滑到受阻才停；未发现卡顿、提前停止或残留输入。 |
| HUD 顶部计数 | `PASS` | Dot/Coin/Star 消失与 HUD 计数同步；收集物提前消失问题修复后回归通过。 |
| 失败弹窗链路 | `PASS` | `eng04_death_validation` 中触碰 Spikes 后进入失败弹窗。 |
| 通关弹窗链路 | `PASS` | 到达 Exit 后先完成移动收尾，再显示通关弹窗。 |
| 重开链路 | `PASS` | 失败后点击“重新开始”回到 Enter，收集物恢复，HUD 清零。 |
| 重复游玩 fallback | `PASS` | 当前仅接入 `story_001` 时，通关按钮重新进入 `story_001`。 |
| 点击区域与缩放 | `PASS` | 开始、重复游玩、重新开始按钮居中；缩放后仍可点击；按钮外点击不误触。 |
| UI 不遮挡可玩区 | `PASS` | HUD 顶部栏位于最上层且有透明度，不影响游玩；作为视觉观察项保留。 |
| 异常恢复 | `NOT_RUN` | 本轮未主动制造加载失败场景；不阻塞 ENG-04/ENG-05 收口。 |

### 9.4 详细记录

#### 9.4.1 默认 `story_001`

| 项目 | 结果 | 观察 |
|---|---|---|
| 启动/loading | `PASS` | 开始界面显示；点击后显示 Loading；Loading 时间很短，不仔细看不明显；能成功进入 `story_001`。 |
| 移动手感 | `PASS` | 四向滑行正常；滑到受阻才停；没有卡顿、提前停止、残留输入。 |
| 收集/HUD | `PASS` | 三类元素会消失；计数同步；顶部栏有透明度不遮挡游玩，但会覆盖在地图元素层级之上。 |
| 通关/重复游玩 | `PASS` | 到达 Exit 会先完成移动收尾，然后弹出通关弹窗；点击按钮后重复游玩重新进入 `story_001`；重进后 HUD 清零。 |
| 点击/缩放 | `PASS` | 开始游玩和重复游玩按钮始终处于浏览器窗口中心；缩放后仍可点击；按钮外点击不会误触发。 |

#### 9.4.2 `eng04_death_validation`

| 项目 | 结果 | 观察 |
|---|---|---|
| 加载 | `PASS` | 打开 `?stage=eng04_death_validation` 后显示开始界面；点击开始后进入验证关卡。 |
| 死亡触发 | `PASS` | 角色滑到 Spikes 后停在尖刺格；随后触发死亡/失败状态。 |
| 失败弹窗 | `PASS` | 出现失败弹窗；淡入期间按钮不可点击，时间间隔较短；淡入完成后按钮可点击。 |
| 重开恢复 | `PASS` | 点击“重新开始”后回到 Enter；收集物恢复；HUD 清零。 |
| 输入屏蔽 | `PASS` | 失败弹窗显示期间方向输入被屏蔽；角色不会继续移动。 |
| 点击/缩放 | `PASS` | 按钮点击区域对齐视觉位置；窗口缩放后仍可点击；按钮外点击不会误触发。 |

### 9.5 问题列表

| ID | 严重级别 | 模块 | 现象 | 处理结果 | 是否阻塞 |
|---|---|---|---|---|---|
| `ENG04-VAL-001` | `P1` | `ENG-04 / ENG-05边界` | 收集物在滑行开始瞬间整条路径提前消失，HUD 计数不同步。 | 已修复为按角色经过格子逐步触发收集；回归 `PASS`。 | 否 |
| `ENG04-VAL-002` | `P3` | `环境` | 浏览器 ES Module 缓存导致一轮回归仍运行旧代码。 | 已通过模块版本 query 与强制刷新规避；记录为后续验收注意项。 | 否 |
| `ENG05-VAL-001` | `P3` | `ENG-05` | Loading 时间很短，不仔细看不明显。 | 本地小 JSON 加载快导致；不影响状态流，暂不处理。 | 否 |
| `ENG05-VAL-002` | `P3` | `ENG-05/视觉` | HUD 顶部栏覆盖在地图元素层级之上。 | HUD 应位于最上层且透明，不影响游玩；作为视觉观察项保留。 | 否 |

### 9.6 回归记录

| 日期 | 修改点 | 回归范围 | 结果 | 备注 |
|---|---|---|---|---|
| `2026-04-26` | 收集物从滑行开始整条路径提前处理，改为按角色经过格子逐步处理 | `story_001` 收集/HUD | `PASS` | 需强制刷新或无痕窗口避免 ES Module 缓存误判。 |
| `2026-04-26` | 新增 `eng04_death_validation` 本地验证关卡和 URL 参数入口 | 死亡、失败弹窗、重开恢复、输入屏蔽、点击缩放 | `PASS` | 默认玩家路径仍为 `story_001`。 |

### 9.7 结论（2026-04-26 当时判断，已被 9.9 与 9.10 重开记录覆盖）

- `ENG-04` 当时状态：`可收口`
- `ENG-05` 当时状态：`可收口`
- 当时是否允许进入下一轮联调：`是`
- 当时是否允许进入最终验收准备：`是`
- 当前覆盖结论：`2026-04-28 runspeed 基线重开后，需重新回归，不再直接作为最终收口依据`

### 9.8 建议动作

- [x] 2026-04-26 当时动作：保持当前实现，进入最终验收准备
- [ ] 优先修复 `ENG-04` 问题，再回归 `ENG-05`
- [ ] 优先修复 `ENG-05` 问题，再继续联调
- [x] 需要同步更新联合验收口径文档
- [x] 需要同步更新执行计划 / worktree 台账

### 9.9 2026-04-27 runspeed 基线重开记录

| 字段 | 内容 |
|---|---|
| 触发原因 | 新版逆向报告确认 `_runSpeed = 5.0` 为 base 值，实际速度需乘 `_gameStageScale = 1.6` |
| 校正后基线 | 实际移动速度 `8.0 tiles/s` |
| 当时代码状态 | 待回改，上一轮实现等效速度仍为 `5.0 tiles/s` |
| 验收状态 | 已被 `2026-04-28` 记录覆盖；保留为历史记录 |
| 下一步 | 以 `2026-04-28` 新基线为准继续处理 |

### 9.10 2026-04-28 runspeed 基线二次重开记录

| 字段 | 内容 |
|---|---|
| 触发原因 | 人工录屏逐帧验证与更新后的逆向报告确认 `8.0 tiles/s` 不是玩家 tile 穿越速度 |
| 校正后基线 | 源速度 `_runSpeed = 5.0 world units/s`；单格尺寸 `TileSize = 0.12 world units/tile`；tile-grid 等效速度约 `41.67 tiles/s` |
| 坐标系决策 | 当时采用最小修正：当前 H5 MVP 代码继续使用 tile-grid 逻辑坐标，world units 仅作为速度来源和换算依据；该决策已被 2026-04-29 三层坐标域方案升级 |
| `_gameStageScale` 处理 | `_gameStageScale = 1.6` 视为舞台/相机缩放相关参数，不参与玩家 tile 位移速度换算 |
| 当时代码状态 | 待回改，上一轮未提交代码仍使用 `8.0 tiles/s` |
| 验收状态 | `2026-04-26` 结果保留为历史记录；不得作为最终收口依据 |
| 下一步 | 先回改 ENG-04 速度代码，再重新执行 `story_001` 与 `eng04_death_validation` 真实浏览器联合验收 |

### 9.11 2026-04-28 runspeed 代码回改记录

| 字段 | 内容 |
|---|---|
| 修改内容 | `PlayerController` 默认速度改为由 `RUN_SPEED_WORLD_UNITS_PER_SECOND / WORLD_UNITS_PER_TILE` 派生 |
| 当前代码基线 | `RUN_SPEED_WORLD_UNITS_PER_SECOND = 5.0`，`WORLD_UNITS_PER_TILE = 0.12`，`DEFAULT_MOVE_SPEED_TILES_PER_SECOND = 41.6666666667` |
| 缓存处理 | `index.html` 与 `main.js` 模块 query 已更新到 `eng04_runspeed_v2` |
| 自动检查 | 默认速度导出与实例化检查通过；`PlayerController.js`、`main.js` 语法检查通过；关卡 JSON 解析通过 |
| 当前验收状态 | 待重新执行 `story_001` 与 `eng04_death_validation` 真实浏览器联合验收 |

### 9.12 2026-04-29 坐标域方案升级记录

| 字段 | 内容 |
|---|---|
| 触发原因 | 最小派生速度修正仍让 `tiles/s` 成为代码主口径，无法清晰表达逆向报告中的 `world units/s` 与 `TileSize` |
| 新方案 | Tile 拓扑坐标负责关卡/碰撞/事件；World 运动坐标负责玩家连续位移、速度和耗时；Screen/Design 像素负责渲染、HUD 和点击 |
| 速度口径 | 主口径为 `_runSpeed = 5.0 world units/s`；`41.6667 tiles/s` 仅为派生显示/验收换算值 |
| 待代码回改 | 新增 `CoordinateSystem`，改 `PlayerController` 为 `worldX/worldZ` 等字段并处理固定步长过冲，改 `Renderer` 适配 world 坐标 |
| 当前验收状态 | `2026-05-01` 已完成真实浏览器回归，结果 `PASS` |
| 回归要求 | 已重新跑 `story_001` 与 `eng04_death_validation`，短距离移动、死亡/通关时序、HUD 同步、输入屏蔽和点击缩放均通过 |

### 9.13 2026-04-30 输入缓冲窗口修正记录

| 字段 | 内容 |
|---|---|
| 触发原因 | 逆向复核确认 `_nextSwipeTimeout = 0.1f`，此前文档沿用的 `0.02s/20ms` 是错误记录 |
| 新基线 | 输入缓冲窗口为 `0.1s/100ms`，单缓冲，新滑动覆盖旧方向 |
| 时序口径 | 缓冲写入发生在移动中收到输入时；缓冲倒计时按 `update(dt)` 递减；撞墙停下时若 `bufferTimer > 0` 立即消费 |
| 待代码回改 | `PlayerController` 默认 `bufferDuration` 改为 `0.1`，并将自然倒计时迁移到 `update(deltaTime)` 路径；`fixedUpdate` 继续负责玩家位移 |
| 回归要求 | 已重新验证 100ms 内预输入可在停下瞬间执行、超过 100ms 不执行、连续多次输入只保留最后一次，结果 `PASS` |

### 9.14 2026-05-01 输入缓冲与坐标域真实浏览器回归记录

| 字段 | 内容 |
|---|---|
| 测试日期 | `2026-05-01` |
| 测试入口 | `http://127.0.0.1:8094/`、`http://127.0.0.1:8094/?stage=eng04_death_validation` |
| 缓存确认 | 两个页面均确认加载 `eng04_input_buffer_v1` |
| `story_001` 主链路 | `PASS`：启动、滑行、收集、HUD、通关、重复游玩与此前整体情况一致 |
| 100ms 输入缓冲 | `PASS`：快速连续滑动体感正常；AHK 毫秒级脚本边界测试无问题 |
| 死亡链路 | `PASS`：`eng04_death_validation` 中 Spikes 死亡、失败弹窗、重开恢复与此前整体情况一致 |
| 弹窗输入屏蔽 | `PASS`：弹窗期间方向输入被屏蔽 |
| 点击与缩放 | `PASS`：按钮点击区域与窗口缩放后位置一致 |
| 总体结论 | `PASS`：代码批次 4-6 的真实浏览器回归通过 |

| 需回归范围 | 原因 | 当前状态 |
|---|---|---|
| 滑行手感主链路 | 速度变化和 100ms 输入缓冲会改变单次滑行耗时与连击体感 | `PASS` |
| 输入缓冲 100ms | R-008 从 20ms 修正为 100ms，需要验证有效、过期、覆盖三类语义 | `PASS` |
| HUD 顶部计数与收集物消失 | 速度变化会压缩逐格收集触发间隔 | `PASS` |
| 通关弹窗链路 | 需确认进入 Exit 后仍先完成移动收尾再弹窗 | `PASS` |
| 失败弹窗链路 | 需确认 Spikes 死亡触发与失败弹窗时序仍稳定 | `PASS` |
| 输入屏蔽与按钮时机 | 需确认弹窗期间输入锁定和淡入按钮锁定仍稳定 | `PASS` |
| 点击区域与缩放 | 作为 ENG-05 联调回归项保留 | `PASS` |
